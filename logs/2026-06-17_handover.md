# Handover — Browser-E2E-Tests reparieren (Stand 17.06.26)

## Kontext
- **Repo/Branch:** MugPlan, Branch `tkt-90-kw-berechnungen` (alles committet + gepusht: `998c266c`, `590728bb`).
- **Auftrag:** Fehlschlagende Browser-E2E-Suites einzeln grün machen. **Nur Test-Code, kein Produktivcode** — Tests werden an das tatsächliche App-Verhalten/aktuelle testids angepasst.
- **Arbeitsweise:** pro Suite zuerst laufen lassen (messen statt raten), dann fixen. Bei echten Verhaltens-/Designentscheidungen **ausführlichen Fließtext** mit Code-/DOM-Belegen + Optionen + Empfehlung liefern und Bestätigung abwarten (nicht knappe AskUserQuestion). Vor Git-Aktionen fragen.
- **Lauf-Befehl:** `npm run test:e2e:browser -- <suite>` (Einzeltest: `-g "WR-02"`). Ergebnis-Output via `tail` lesen; bei rotem Test die `test-results/.../error-context.md` (DOM-Snapshot) ansehen.

## Stand: 11 Suites grün
appointment-form.resource-conflicts, appointment-form.layout-tour-integration, appointment-form.tour-change-dialog, appointments-list.tour-employee, calendar-move.cross-context-conflicts, calendar-cut-paste.resource-conflicts, calendar-drag-drop.success, calendar-markers-visualization, employee-appointments-utilization, tour-week-form, tour-week-planning.employee-add-cascade.

## Offen — konkrete nächste Schritte

### 1. tour-week-planning.employee-remove-cascade (WR-02 bis WR-07)
WR-01 ist grün, die **Helper sind bereits umgebaut**. WR-02..07 haben noch veraltete testids. Muster zum Anwenden (analog zur grünen WA-Suite):
- `week-plan-remove-appointment-${id}` → `tour-employee-cascade-row-${id}`
- Badge sichtbar: `page.getByTestId(\`week-tour-planning-panel-${tour.id}\`).getByTestId(\`week-planning-employee-badge-${id}\`)` → `page.getByTestId(\`week-personnel-employee-tour-${tour.id}-${id}\`).first()`
- Badge abwesend (`toHaveCount(0)`): `week-personnel-employee-tour-${tour.id}-${id}` **ohne** `.first()`
- WR-06 "kein Remove-Button": `week-planning-employee-remove-${id}` → `week-personnel-employee-tour-${tour.id}-${id}-remove` mit `.toHaveCount(0)`
- Danach Scope-Header der WR-Datei aktualisieren (wie bei WA).

### 2. users-management-scroll
Noch nicht angeschaut. Erst laufen lassen, Fehler ansehen.

### 3. Gesamtverifikation
Danach vollständiger Browser-E2E-Lauf.

## Wichtige UI-Fakten (echte testids — sparen Neuanalyse)
- **Wochenplanungs-Spalte** (linke Spalte im Wochen-View, standardmäßig aus): einblenden `switch-week-personnel-column`, erweitern `button-week-personnel-column-toggle-tour-${id}` (collapse-Zustand **persistiert** zwischen Tests → Helper macht conditional toggle), Hinzufügen `button-add-week-personnel-tour-${id}`, Badge `week-personnel-employee-tour-${id}-${empId}` (+ `-remove`).
- **Mehrere Wochen gleichzeitig sichtbar** → `.first()` trifft die Zielwoche (nach `navigateWeekOffset`).
- **Mitarbeiter-Picker:** `employee-picker-card-${id}` + **dblclick** (Card-/Board-Ansicht ist Default).
- **Cascade-Dialog** (add+remove): `dialog-tour-employee-cascade`, `tour-employee-cascade-row/checkbox/status-${appointmentId}`, `button-tour-employee-cascade-confirm`, Abbrechen via `getByRole("button",{name:"Abbrechen"})`. Status-Texte: Konflikt "Überschneidung mit bestehendem Termin", frei "Wird zum Termin hinzugefügt".
- **Proaktive Konfliktvermeidung (Weg A, vom Nutzer bestätigt):** Der Picker (`/api/tours/{id}/week-employees/available`) bietet in der Woche belegte Mitarbeiter gar nicht erst an — Konflikt-Tests prüfen `employee-picker-card-${id}` `.toHaveCount(0)`, nicht einen Konfliktdialog.

## Wiederkehrende Fix-Muster (bereits angewandt)
- MA-loser Kalender-Move/Cut-Paste → immer "Trotzdem verschieben"-Dialog, kein stiller Direkt-Move.
- Tourwechsel bestehender Termine → `dialog-appointment-move` (mehrstufig); neue Termine → `dialog-tour-employee-cascade`.
- HoverPreview öffnet erst nach Trigger-Initialisierung (Marker: Breitenvarianten durchschalten; cursor-mode-Bars: zweite Cursorbewegung im Element).
- Echtes `dragTo` im Monatssheet → `targetPosition` auf den freien Kopfbereich (Termin-Overlays blockieren die Tag-Mitte).
- DB-Date-Vergleiche: `new Date(x).toISOString().slice(0,10)`.

## Referenzen
- FS-Log: `logs/2026-06-17_browser-e2e-ui-anpassung.md`
- Projekt-Manager-Kommentar an PROJ-1: ID 116
- `gitleaks-tmp-report.json` ist bewusst untracked (temp-Artefakt, nicht committen).
