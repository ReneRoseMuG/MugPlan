# Kritische Hinweise – Projektstatus-Badges & Modal-Auswahl

- Weitere `ColoredInfoBadge`-Nutzungen gefunden (`EmployeeList`, `EmployeePage`, `LinkedProjectCard`). Diese rendern keine Projektstatus mit Entfernen-Funktion, daher bleibt das Legacy-Verhalten unverändert.
- Projektstatus-Badges im Projektformular wurden explizit auf den Remove-Modus der InfoBadge-Vorlage umgestellt, damit der "-"-Button angezeigt wird.
